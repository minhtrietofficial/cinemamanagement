<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h4>Chọn ghế ngồi</h4>
    <div id="A">
    {{#each ghe}}
            {{#if (hangA SoGhe)}}
                {{#if (empty TrangThai)}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}">
                {{else}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}" disabled>
                {{/if}}
            {{/if}}
    {{/each}}
    </div>
    <div id="B">
    {{#each ghe}}
            {{#if (hangB SoGhe)}}
                {{#if (empty TrangThai)}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}">
                {{else}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}" disabled>
                {{/if}}
            {{/if}}
    {{/each}}
    </div>
    <div id="C">
    {{#each ghe}}
            {{#if (hangC SoGhe)}}
                {{#if (empty TrangThai)}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}">
                {{else}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}" disabled>
                {{/if}}
            {{/if}}
    {{/each}}
    </div>
    <div id="D">
    {{#each ghe}}
            {{#if (hangD SoGhe)}}
                {{#if (empty TrangThai)}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}">
                {{else}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}" disabled>
                {{/if}}
            {{/if}}
    {{/each}}
    </div>
    <div id="E">
    {{#each ghe}}
            {{#if (hangE SoGhe)}}
                {{#if (empty TrangThai)}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}">
                {{else}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}" disabled>
                {{/if}}
            {{/if}}
    {{/each}}
    </div>
    <div id="F">
    {{#each ghe}}
            {{#if (hangF SoGhe)}}
                {{#if (empty TrangThai)}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}">
                {{else}}
                    <label for="">{{SoGhe}}</label>
                    <input type="checkbox" name="" id="" value="{{SoGhe}}" disabled>
                {{/if}}
            {{/if}}
    {{/each}}
    </div>
    <div>
        <button onclick="getGhe()">Xác nhận</button>
        <button onclick="cancelDatVe()">Hủy</button>
    </div>
</body>
<script src="/javascripts/user.js"></script>
</html>